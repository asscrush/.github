name: Update Profile Index

on:
  workflow_dispatch:

jobs:
  update:
    uses: assforge/gitflow-packages/.github/workflows/update-org-profile.yml@main
    with:
      org_name: asscrush
      org_emoji: "💎"
      org_title: "Common Packages"
      org_subtitle: "Microsb Shared Libraries"
      section_title: "Packages"
    secrets:
      ORG_READ_TOKEN: ${{ secrets.ORG_READ_TOKEN }}
